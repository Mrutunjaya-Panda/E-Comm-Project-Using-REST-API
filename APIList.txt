next set of API creation

//These are accessed or used when user Logs In(This is where we will look how to secure the API and how to use JWT token for authentication and authorization)
like:-
1. Rate Product (this is a user specific API, only logged in user can rate the product and we will use JWT token to identify the user and allow them to rate the product)
2. Add items to Cart 
3. Get items of Cart 
4. Remove items from cart.

//two APIS
UserController
1. signUp - Email, Name, Password, TypeOfUser(customer, seller)
2. signIn/login - (Email, Password)